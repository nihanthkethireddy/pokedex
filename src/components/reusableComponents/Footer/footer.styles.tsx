import { css } from 'glamor'

let footerStyles = css({
    marginTop: '10px',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'center',
    height: '10%',
    // position: 'absolute',
    bottom: '0',
    ' p': {
        margin: '2px',
    }
})

export { footerStyles }